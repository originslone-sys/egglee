/**
 * config.js — Frontend configuration
 *
 * Set the Cloud Run backend URL here after deploying.
 * Leave commented out if frontend and backend are on the same origin.
 */

// ⚠️ CHANGE THIS to your Cloud Run URL after deploying:
// window.GALINHA_API_URL = 'https://galinha-farm-XXXXX-uc.a.run.app/api';
