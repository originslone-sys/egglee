# Bloqueia acesso web a esta pasta (apenas uso interno pelo PHP)
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order allow,deny
  Deny from all
</IfModule>
