# Segredos / estado do servidor (criados em runtime, não versionar)
.env
.env.local
database/.installed
database/last-run.json

# Dependências / build (legado Astro, caso reapareçam)
node_modules/
dist/
.astro/

# Sistema
.DS_Store
*.log
