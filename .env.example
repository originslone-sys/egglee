# Copie para .env e preencha. NUNCA versione o .env real.
DEEPSEEK_API_KEY=sk-sua-chave-aqui
# Opcionais (têm padrão sensato):
# Modelos atuais: deepseek-v4-flash (rápido) ou deepseek-v4-pro (mais capaz).
DEEPSEEK_MODEL=deepseek-v4-flash
# thinking mode: "disabled" (resposta direta, recomendado) ou "enabled".
DEEPSEEK_THINKING=disabled
# teto de tokens da geração (mais = artigo maior, porém mais lento).
DEEPSEEK_MAX_TOKENS=5000
DEEPSEEK_API_URL=https://api.deepseek.com/chat/completions
SITE_URL=https://seu-dominio.com

# Imagens (Pexels) — crie grátis em https://www.pexels.com/api/
PEXELS_API_KEY=

# ---- Monetização e contato (opcionais) ----
# AdSense só carrega com o client preenchido E o visitante aceitando os cookies.
ADSENSE_CLIENT=
# Slot opcional, para o anúncio no meio do artigo (sem ele, use Auto Ads).
ADSENSE_SLOT=
# E-mail exibido nas páginas legais (privacidade, contato, etc.).
CONTACT_EMAIL=

# ---- Analytics e Search Console (opcionais) ----
# Google Analytics 4 (G-XXXXXXXXXX) — só carrega após o consentimento de cookies.
GA_ID=
# Search Console: valor do atributo content da meta google-site-verification.
GSC_VERIFICATION=

# ---- MySQL ----
DB_HOST=localhost
DB_PORT=3306
DB_NAME=seu_banco
DB_USER=seu_usuario
DB_PASS=sua_senha

# ---- Cache de página inteira (opcional) ----
# Liga ("1", padrão) ou desliga ("0"). Cacheia páginas públicas para visitantes
# sem cookie de consentimento (bots e primeira visita) — acelera e reduz carga.
CACHE_ENABLED=1
# Validade do cache em segundos (padrão 21600 = 6h).
CACHE_TTL=21600

# Debug (mostra stack trace em erro 500). Use "0" em produção.
APP_DEBUG=0
