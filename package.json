{
  "name": "egglee",
  "type": "module",
  "version": "0.1.0",
  "description": "Site multilíngue de interpretação de sonhos — conteúdo estruturado, SEO técnico e geração assistida por IA com revisão humana.",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "generate": "node scripts/generate.mjs",
    "generate:one": "node scripts/generate.mjs --only",
    "validate": "node scripts/validate.mjs"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.2.1",
    "astro": "^4.16.0"
  },
  "devDependencies": {
    "zod": "^3.23.8"
  }
}
